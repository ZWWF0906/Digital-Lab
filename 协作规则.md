# 协作规则（Agent 执行约定）

适用范围：本仓库（DigitalLab）的所有 Agent 任务。

允许超范围发挥。若判断有必要做指令范围之外的改动，可直接做，不必停下等待。但必须满足：

一、所有超出指令范围的改动，必须在报告里单独列出，写明做了什么、为什么这么做。不能混在改动清单里，不能省略。

二、涉及以下不可逆操作，仍必须停下先问：删除文件、修改打包配置、修改签名与证书、修改 .gitignore 的忽略规则、推送远程。

三、如果超范围改动引入了新问题，如实说明，不要掩饰。

四、报告中额外改动部分要放在显眼位置，不要藏在末尾。

五、诚实报备的完整程度，比改动本身更重要。
