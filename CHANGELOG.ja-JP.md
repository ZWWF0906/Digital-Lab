# 変更履歴

## v1.4.0 - 2026-09-21

### 追加

- 多言語インターフェース：簡体字中国語・英語・日本語に対応し、設定からいつでも切り替え可能

- 初回起動ガイド：インターフェース言語の選択、NAS デバイスの追加、接続確認を行う 3 ステップのガイド

- しきい値アラートのシステム通知：CPU・メモリ・ディスクの使用率が設定したしきい値を超え続けた場合にデスクトップ通知を表示し、値がしきい値を下回るまで同じアラートは繰り返し通知されない

- AI アシスタントが、選択したインターフェース言語で回答するようになった

### 修正

- AI のエラーが明確になり、選択したインターフェース言語で表示されるようになった：サービスに接続できない、モデルが存在しない、認証情報が拒否されたなどの状況で、何が起きたかと次に何をすべきかを説明する

## v1.3.0 - 2026-09-18

### 追加

- 起動スプラッシュアニメーション：六角形と波形のストローク描画、ワードマークの出現、セルフチェック項目の段階的な点灯、締めくくりのブリーズとフェードアウト

- 起動スプラッシュがダークテーマとライトテーマの両方に対応

- 設定に起動スプラッシュのオン/オフスイッチを追加

- AI アシスタントのヘッダーに「クイックデプロイ」のプレースホルダーボタンを追加

### 変更

- Orbitron、Inter、JetBrains Mono をアプリに同梱し、システムフォントや Web フォントに依存しなくなった

- ウィンドウ、タスクバー、トレイ、ストア用のアプリアイコンを描き直した

### 修正

- 起動アニメーションが早く始まりすぎる問題を修正：ウィンドウが表示された時点で六角形のストローク進捗が 0% のままになった

- プロセスを強制終了した後も、選択したテーマが保持されるようになった

- アプリの設定ファイルがプロジェクトフォルダに書き込まれなくなった

- タスクバーの識別情報がインストール済みショートカットと一致するようになった

## v1.2.0 - 2026-09-10

### 追加

- AI ローカルメモリ：アシスタントがユーザーの好みを記憶し、システムプロンプトに注入できるようになった（設定でオン/オフ、保存ディレクトリ、一覧表示・削除・全消去が可能）

### 変更

- CLI コマンドを整理した（プレースホルダーと旧コマンドを削除）

### 修正

- システムダイアログでアプリ名が DigitalLab と表示されるようになった
- AI 設定の保存時に、空の入力で保存済みの値を上書きしなくなった
- AI の返信が空の場合、空の吹き出しではなくプレースホルダーを表示するようになった
- メモリ一覧を 2 回クリックしなくても表示できるようになった（設定読み込みの競合）
- NAS プロセステーブルのヘッダー位置ずれを修正
- メモリの削除にネイティブの警告ダイアログを使用するようになった

## v1.1.1 - 2026-09-05

### 修正

- AI チャットの返信で中国語が文字化けする問題を修正

## v1.1.0 - 2026-09-04

### 追加

- ライトテーマ（ホワイトモード）：300ms のなめらかなトランジション付き、既定はダークのまま

- 設定パネルにテーマ切り替えを追加し、選択内容は再起動後も保持される

- 多層のライト表面（ページ / サイドバー / カード / ホバー）：コントラストを調整した配色

- ライトモードでは、ダークのグロー表現に代えて 4 段階のグレーシャドウ体系を採用

- AI アシスタントのシステムプロンプト：役割設定 + 中国語での回答を必須化

- AI のストリーミングデコードと繰り返し出力の防止により、文字化けや暴走を抑制

### 変更

- ステータス色（エメラルド / アンバー / レッド）をライト背景向けに再調整

- ターミナルパネルは両テーマでダークのまま（テーマ非依存のトークン）

- ライトモードでは境界線を中立色に統一し、エメラルドはアクティブ状態とフォーカス状態にのみ使用

- ライトモードでの可読性向上のため、テーブルのゼブラ縞・行ホバー・スクリムを再構築

## v1.0.0 - 2026-08-15

### 追加

- 最初の安定版

- Electron + Python のハイブリッドアーキテクチャ

- リアルタイムのシステム監視（CPU、メモリ、ディスク、ネットワーク）

- ハードウェア情報センター（CPU、GPU、メモリ、ディスク、ディスプレイ、ネットワーク、システム）

- NAS の SSH 管理（リモートターミナル、パフォーマンス監視）

- AI アシスタント（Ollama / OpenAI 互換 API、ストリーミング出力）

- 設定管理システム（二層ストレージ、機密情報の分離）

- システムトレイとトレイへの格納

- 単一インスタンスロック

- ハードウェアアクセラレーションのスイッチ（仮想マシン互換のため既定はオフ）

- Python プロセスの自動再起動（最大 3 回）

### アーキテクチャ

- `system_state` による統一データモデル

- JSON Lines による IPC 通信（stdin/stdout）

- モジュール式パネルシステム（ダッシュボード、デバイスセンター、ターミナル、AI アシスタント、設定）

- 組み込みの Python バックエンド

- Flask Web Dashboard（オプション）

### 既知の制限

- SSH ターミナルは自動再接続に対応していない

- AI アシスタントは対話のみで、Agent ツール呼び出しには対応していない

- NAS 監視は Linux ベースの NAS のみに対応（SSH コマンド経由）

- 仮想マシンとの互換性のため、ハードウェアアクセラレーションは既定で無効
